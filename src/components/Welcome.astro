---
import { Image } from "astro:assets";
import brillo from "../assets/brillo.gif";
---

<section class="presentation">
  <div class="presentation-container">
    <Image src={brillo} alt="Estrellas" class={"presentation-image-one"} />
    <div class="presentation-main-text">
      <h1>Elena Expósito</h1>
      <h2>Desarrolladora Web</h2>
    </div>
    <Image src={brillo} alt="Estrellas" class={"presentation-image-two"} />
  </div>
  <h3>Portfolio</h3>
</section>

<style>
  @font-face {
    font-family: "Gliker";
    src: url("@src/fonts/Gliker-Regular.ttf") format("truetype");
    font-weight: normal;
    font-style: normal;
  }

  @font-face {
    font-family: "Afacad Italic";
    src: url("@src/fonts/Afacad-Italic[wght].ttf") format("truetype");
    font-weight: normal;
    font-style: normal;
  }

  body {
    height: 100vh;
    background-repeat: no-repeat;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  .presentation {
    position: relative;
    z-index: 1;
    height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    background: transparent;
    padding: 1.5rem;
  }

  .presentation-container {
    display: flex;
    justify-items: center;
    text-align: center;
    gap: 2rem;
  }

  .presentation-image-one {
    display: flex;
    align-self: flex-end;
  }
  :root {
    --h1-size: 6rem;
    --h2-size: 5rem;
    --h3-size: 2rem;
  }

  h1,
  h2 {
    font-family: "Gliker", sans-serif;
    color: #0e0c14;
  }

  h1 {
    font-size: var(--h1-size);
  }

  h2 {
    font-size: var(--h2-size);
  }

  h3 {
    font-family: "Afacad Italic", serif;
    font-size: var(--h3-size);
    padding: 0.5rem;
    border: black 2px solid;
    text-align: center;
    box-shadow: 1px 1px 2px rgba(0, 0, 0, 0.4);
    background-color: #e76f51;
  }

  h3:hover {
    background-color: #2a9d8f;
    cursor: pointer;
  }

  @media (max-width: 1200px) {
    :root {
      --h1-size: 4rem;
      --h2-size: 3rem;
      --h3-size: 1.5rem;
    }
  }

  @media (max-width: 768px) {
    :root {
      --h1-size: 3rem;
      --h2-size: 2rem;
      --h3-size: 1.2rem;
    }

    .presentation-container {
      gap: 0.5rem;
    }

    .presentation-image-one,
    .presentation-image-two {
      width: 30px;
      height: 30px;
    }
  }
</style>
